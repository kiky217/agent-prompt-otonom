"""HERMES Bootstrap Sync preview package.

Public helpers live in :mod:`bootstrap_sync.bootstrap_sync`. Keeping package
initialization side-effect free also makes ``python -m`` execution predictable.
"""
